# wsi-processing-pipeline
Scalable data pipeline for whole-slide image (WSI) preprocessing and feature extraction on BU's HPC cluster. Orchestrates tissue segmentation, patch extraction, and embedding generation via TRIDENT, with downstream model training via Patho-Bench. Config-driven execution, batch processing, automated storage management, and reproducible runs.
